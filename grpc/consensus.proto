edition = "2023";

option go_package = "./consensus";

service Node {
  rpc Request(Message) returns (Reply) {}
  rpc Respond(Message) returns (Released) {}
}

message Message {
  int64 timestamp = 1;
  int64 id = 2;
}

message Reply {
  bool isQueued = 1;
}

message Released {}